---
- name: Common tasks FTW
  hosts: all
  become: yes
  pre_tasks:
    - name: Set hostname
      hostname: name={{inventory_hostname}}

- name: Base static-panda service
  hosts: base
  become: yes
  roles:
    - { role: flask, tags: flask }
